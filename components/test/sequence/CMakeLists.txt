add_library(sequence SHARED sequence.cpp)
set_target_properties(sequence PROPERTIES OUTPUT_NAME "component" PREFIX "")
if(APPLE)
target_link_libraries(sequence brahms-engine brahms-engine-base)
endif(APPLE)
