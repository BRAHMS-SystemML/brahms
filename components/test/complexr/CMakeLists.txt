add_library(complexr SHARED complexr.cpp)
set_target_properties(complexr PROPERTIES OUTPUT_NAME "component" PREFIX "")
if(APPLE)
target_link_libraries(complexr brahms-engine brahms-engine-base)
endif(APPLE)
